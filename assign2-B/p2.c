#include <stdio.h>#include <unistd.h>#include <stdlib.h>#include <string.h>int main(int argc,char *argv[]){	          int first,last,middle,n,i,search;	  int a[20] ;	  	  printf("Sucessfully:%d\n",(argc));	  for(i = 0 ;i < argc ;i++)	  {	     a[i]=atoi(argv[i]);	  }	  	  printf("\nPrinting Elements:-\n");	  for(i=0;i<argc;i++)	  {	     printf("%d\t",a[i]);	  }	  printf("\nEnter Key to be searched:");	  scanf("%d",&search);	 	  n=argc;	  first = 0;	  last = n - 1;	  middle = (first+last)/2;	 	   while (first <= last) 	   {	      if (a[middle]<search)	      {		 first = middle + 1;    	      }	      else if (a[middle]==search) 	      {		 printf("\n%d found at location %d.\n", search, middle+1);		 break;	      }	      else	      {		 last = middle - 1;	      }	 	 	      middle = (first + last)/2;	   }	   if (first > last)	   {	      printf("\nNot found! %d isn't present in the list.\n", search);	   }	 	  return 0;}